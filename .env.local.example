# Copy this file to .env.local and fill in your own values.
# .env.local is gitignored and was deliberately NOT included in this zip —
# it holds a live API key that must not travel with shared source.

GEMINI_API_KEY=your-key-here
GEMINI_MODELS=your-model-list-here
